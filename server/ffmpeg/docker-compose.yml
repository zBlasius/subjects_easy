version: "3.9"

services:
  api:
    build: .
    container_name: subjects-easy-ffmpeg
    ports:
      - "3001:3001"
    environment:
      AWS_REGION: eu-north-1
      AWS_ACCESS_KEY_ID: ""
      AWS_SECRET_ACCESS_KEY: ""
